const CardClusterTitle = ({ title, subtitle }) => (
  <div className="text-left md:text-center p-2 mt-16 mb-10">
    <div className="text-lg lg:text-2xl font-bold">{title}</div>
    <div className="text-text lg:text-base pt-3">{subtitle}</div>
  </div>
)

export default CardClusterTitle
